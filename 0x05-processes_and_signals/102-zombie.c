#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int main(void)
{

    // pid_t pid;
    // if (pid == -1)
    // {
    //     perror("fork");
    //     exit(-1);
    // }
    // else
    // {
    //     for (size_t i = 0; i < 5; i++)
    //     {
    //         pid = getpid();
    //         printf("Zombie process created, PID: %d\n", pid);
    //     }
    // }
}